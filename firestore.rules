rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all read, write, and delete operations until Jan 1, 2026 for development purposes.
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2026, 1, 1);
    }
  }
}
